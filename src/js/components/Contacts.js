var React = require('react');

var Contacts = React.createClass({
	render: function() {
		return (
			<div className="contacts">
				<h1>Contacts</h1>
				<p>Contacts page content.</p>
			</div>
		);
	}
});

module.exports = Contacts;